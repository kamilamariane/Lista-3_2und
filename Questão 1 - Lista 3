#include<stdio.h>

int main(){  
int soma=0; //soma
int i; //contador 

for(i=0; i<=100; i++){
  //soma = i + soma;
  //or
  soma+=i;
}
printf(" A soma dos 100 primeiros numero naturais é %d", soma);
 return 0;
 }
