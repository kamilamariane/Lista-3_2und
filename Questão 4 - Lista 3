#include<stdio.h>

int main(){  
int divnum; 
int num; //numero que o usuario entra
int i; //contador 

printf(" Digite um num: "); scanf(" %d", &num);

for( i=1; i<=num; i++){
  if( num % i == 0){
    printf (" %d\n", i);
  }
}

 return 0;
 }
