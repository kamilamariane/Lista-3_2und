#include <stdio.h>
#include <stdlib.h>

int main(){
  int vetor[10];
  int x, i;
  
  printf ("digite 10 numeros\n");

  for (i = 0; i <10; i++)   /*Este laco faz o scan de cadaelemento do vetor*/
   {
     scanf("%d", &vetor[i]);
  }
 i = 1;
 //x = vetor[0];
printf(" os numeros no intervalo [10,50] são:\n");
 while(i<10){ 
 if(vetor[i]<=50 && vetor[i]>=10){
     printf("%d\n", vetor[i]);
 }
 i++;
 }
 return 0;
}
